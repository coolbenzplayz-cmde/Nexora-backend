package org.example.nexora.messaging;

/**
 * Call permission enum
 */
public enum CallPermission {
    INITIATE_CALL,
    RECEIVE_CALL,
    SCREEN_SHARE,
    RECORD_CALL,
    MUTE_PARTICIPANTS,
    KICK_PARTICIPANTS,
    ADMIN_CONTROLS
}
